# 🚀 دليل المعمارية والمواصفات المعتمد لمشروع "منصة خطة" (Khotta / L-Start Investor Platform)

> **الغرض من هذا المستند:** يوفر هذا الملف تحليلاً شاملاً وهيكلياً لمشروع منصة "خطة" لاستخدامه كمرجع مرجعي (Context Master) عند النقاش والتحاور مع نماذج الذكاء الاصطناعي (LLMs) أو فرق التطوير، بهدف توسعة المنصة، إضافة الميزات، أو إجراء الصيانة المعمارية.

---

## 1. 📌 ملخص المشروع والرؤية العامة (Executive Summary)

**منصة خطة (Khotta / L-Start Investor)** هي منصة إلكترونية تفاعلية متكاملة موجّهة لرواد الأعمال، المستثمرين، والمستشارين في الوطن العربي. تهدف المنصة إلى تحويل عملية "إعداد دراسات الجدوى واستكشاف الفرص الاستثمارية" من عملية معقدة وطويلة إلى رحلة تفاعلية مدعومة بالبيانات والذكاء الاصطناعي.

### **أبرز الركائز الأساسية:**
1. **منهجيات دراسة الجدوى المتعددة:**
   - **النموذج السهل (Easy Plan):** إعداد سريع خطوة بخطوة للمشاريع الصغيرة والمتوسطة.
   - **النموذج الاحترافي (Pro Plan):** تحليل استراتيجي ومالي وتدفقات نقدية معمقة.
   - **MIT 24 Steps:** تطبيق منهجية معهد MIT لبناء الشركات التكنولوجية الناشئة والقابلة للتوسع.
   - **نموذج العمل BMC (Business Model Canvas):** لوحة تفاعلية لبناء وتعديل نموذج العمل التجاري.
2. **رادار استكشاف الأسواق والقطاعات (Market Discovery):**
   - أكثر من 100+ لوحة بيانات قطاعية (Sector Dashboards) متخصصة تغطي مجالات التجزئة، التكنولوجيا، الزراعة، العقارات، الطاقة، وغيرها.
   - محرك المشكلات والفرص (Problem & Opportunity Engine).
   - رادار اليونيكورن (Unicorn Benchmarking) لمقارنة المشاريع بمعايير الشركات العالمية.
3. **أدوات المساندة والتصميم:**
   - استوديو الهوية البصرية (Brand Identity Studio).
   - معرض المشاريع الناجحة وتحليل أسباب فشل المشاريع (Proven & Failed Projects Analysis).
   - مكتبة أفكار SaaS و Micro-SaaS.

---

## 2. 🛠️ التكنولوجيا المستخدمة (Tech Stack & Ecosystem)

| الفئة | التكنولوجيا / المكتبة | الغرض والوصف |
|---|---|---|
| **الكور الأساسي** | **React 19 + TypeScript + Vite 6** | أداء استثنائي، تحميل سريع، وبناء نماذج عالية الجودة |
| **التنسيقات والنمط** | **Tailwind CSS v4 + Vanilla CSS** | تصميم مرن وعصري متوافق مع RTL (من اليمين إلى اليسار) |
| **مكونات UI الأساسية** | **shadcn/ui + Radix UI Primitives** | مكونات واجهة مستخدم ناعمة وسهلة الوصول (Accessible UI) |
| **الأيقونات والرسم** | **Lucide React + Recharts** | أيقونات حديثة ورسوم بيانية تفاعلية للبيانات المالية والسوقية |
| **التوافقية اللغوية** | **Arabic-first RTL System** | واجهة مصممة خصيصاً للغة العربية دون انزياح أو مشاكل محاذاة |

---

## 3. 📐 المعمارية الهيكلية للكود (Project Architecture & Structure)

```
c:\xampp\htdocs\l-start-investor\
├── src/
│   ├── components/
│   │   ├── layout/            # المكونات الهيكلية الرئيسية (LandingNavbar, Header, Sidebar, BottomNavBar)
│   │   ├── ui/                # مكونات shadcn/ui القياسية (Button, Card, Dialog, Table, Input, Dropdown...)
│   │   ├── views/             # الصفحات الحاكمة (DashboardRouter, Home, MyProjects, LandingPage...)
│   │   ├── features/          # الموديولات الوظيفية المتقدمة:
│   │   │   ├── business/      # محرر دراسات الجدوى، NewPlan, BMC Editor
│   │   │   ├── discovery/     # DiscoveryCenter, UnicornBenchmarking, ProblemEngine
│   │   │   ├── branding/      # BrandIdentityStudio
│   │   │   └── workspace/     # ProjectWorkspaceContext
│   │   └── sectors/           # 100+ لوحة بيانات قطاعية منظمة داخل مجلدات (Ecommerce, Agriculture, Energy...)
│   ├── types/                 # تعريفات TypeScript للشخصيات، المشاريع، والبيانات
│   ├── lib/                   # المساعدين والوظائف البرمجية (utils.ts, cn helper)
│   ├── index.css              # التنسيقات العامة والتغيّرات المتوافقة مع RTL و Shadcn
│   └── App.tsx                # جذر التطبيق مع SidebarProvider و Layout Container
```

---

## 4. 🔀 نظام التوجيه وإدارة الحالة (Routing & State Management)

### **نظام التوجيه التكيّفي (Adaptive View Router):**
- يعتمد المشروع حالياً على نظام توجيه ذكي وخفيف عبر `DashboardRouter.tsx` والخاصية `activeTab`.
- يدعم التحميل الكسول (Lazy Loading / Suspense) لصفحات القطاعات المعقدة لضمان سرعة تحميل أولية ممتازة.
- يدعم حالات الصفحات المختلفة: `loading`, `empty`, `error`, `success`.

### **إدارة الحالة (State Management):**
- **`ProjectWorkspaceContext`**: السياق المركزي للمشروع الحالي، البيانات المالية، الأقسام المكتملة، والهوية البصرية.
- **`SidebarContext`**: التحكم في حالة توسعة وطي القائمة الجانبية مع ضمان ثبات الأبعاد ومنع انزياح الشاشة (Layout Overflow Prevention).

---

## 5. 🎨 قواعد التصميم والمحاذاة (RTL & UI Guidelines)

عند النقاش حول تطوير عناصر الواجهة، يجب الاتزام بالقواعد البرمجية التالية المحددة في `AGENTS.md`:

1. **RTL Arabic-First:** المحاذاة والأيقونات والمسافات موجهة للغة العربية تلقائياً.
2. **اعتماد Shadcn/ui:** يمنع إنشاء مكونات UI مخصصة بديلة إذا كان المكون موجوداً في `src/components/ui/`.
3. **التحكم بالاستجابة (Responsive Standards):**
   - استهداف كل الشاشات: الجوال (`sm`), التابلت (`md`), المكتبي (`lg/xl`), والشاشات العريضة جداً (`2xl`).
   - استقرار الـ Viewport عبر استخدام `overflow-x: clip` بدلاً من `overflow-x: hidden` على مستوى الحاويات لمنع كسر خصائص `position: sticky`.
4. **جماليات الواجهة:** الألوان مستمدة من متغيرات CSS السيمانتية (`primary`, `muted`, `accent`) وتجنب الألوان الفاقعة غير المتناسقة.

---

## 6. 📊 الموديولات الوظيفية الرئيسية في المنصة

### **1. محرك إعداد الخطة (Business Plan Engine):**
- محرر مقسم لأقسام استراتيجية ومالية.
- يدعم التصدير وتوليد المحتوى بالذكاء الاصطناعي.
- مزود بـ `PlanHeader` وأداة حفظ مسودة العمل تلقائياً.

### **2. مركز استكشاف القطاعات (Sector Discovery Center):**
- ينقسم إلى 12 فئة رئيسية (تكنولوجيا، زراعة، عقارات، صناعة، خدمات، إلخ).
- يحتوي كل قطاع على لوحة تحكم تفاعلية (Dashboard) تعرض:
  - أهم الأرقام والمؤشرات الحيوية.
  - تحليل SWOT التفاعلي.
  - قائمة بالفرص والتهديدات وسلسلة القيمة.

### **3. استوديو الهوية البصرية (Brand Identity Studio):**
- بناء اسم المشروع، الشعار اللفظي (Slogan)، لوحة الألوان (Color Palette)، وشخصية العلامة التجارية (Brand Personality).

### **4. شريط الملاحة الرئيسي والصفحة التعريفية (LandingNavbar & LandingPage):**
- هيدر ذكي مزود بـ **Mega Menu** احترافي يضم 3 أعمدة (بناء الخطة، استكشاف السوق، قسم مميز مع CTA).
- توافق كامل مع كافة أحجام الشاشات مع Drawer جانبي على الجوال.

---

## 7. 🤖 دليل صياغة الأوامر للذكاء الاصطناعي (Prompt Guide for LLM Discussions)

استخدم المقاطع التالية عند طلب تعديل أو إضافة ميزات جديدة من أي نظام ذكاء اصطناعي (ChatGPT, Claude, Gemini, Antigravity):

### **صيغة طلب إضافة لوحة قطاع جديد (New Sector Dashboard):**
> *"يرجى إنشاء لوحة بيانات قطاعية جديدة باسم `[اسم القطاع]` داخل مجلد `src/components/sectors/[الفئة]/`. يجب أن تتبع نفس معايير Shadcn/ui و Lucide React والتصميم المعتمد في المشروع، وتدعم التجاوب مع شاشات الجوال والشاشات العريضة `2xl` مع استخدام `onBack` للعودة إلى `market-discovery`."*

### **صيغة طلب تعديل في محرر الخطة أو الواجهة:**
> *"نحن نعمل على مشروع React 19 + TypeScript + Tailwind CSS v4 مع نظام RTL عربي. يرجى تعديل مكون `[اسم المكون]` بحيث يعتمد على `src/components/ui/` ومراعاة عدم التسبب في انزياح أفقي (Layout Shift) واستخدام `cn()` للتنسيق."*

---

## 8. 🗺️ خطة التطوير المستقبلي (Future Roadmap)

1. **الربط مع خدمات الذكاء الاصطناعي (AI Backend Integration):**
   - ربط نماذج توليد النصوص والتحليل المالي المباشر عبر API (مثل OpenAI / Gemini API).
2. **نظام التصدير المتقدم (PDF / PPTX Export Engine):**
   - إمكانية تصدير دراسة الجدوى كاملة بتنسيق طباعي فاخر باللغة العربية.
3. **لوحة تحكم المستخدم المستثمر (Investor Pitch Portal):**
   - ميزة مشاركة دراسة الجدوى عبر رابط آمن مع المستثمرين أو الجهات التمويلية.

---
*تم إنشاء هذا المستند ليكون الدليل المرجعي الهيكلي لمشروع "منصة خطة".*
